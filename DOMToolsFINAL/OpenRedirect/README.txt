To use this scanner:

Copy the entire code above and paste it into your browser's console
Run the scanner:

findRedirectionSinks();

Start watching for dynamic changes:

const observer = watchDynamicChanges();

To stop watching for changes:

observer.disconnect();